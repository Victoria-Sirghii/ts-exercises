//Create a type for user using Admin interface

interface Admin {
  username: string;
  password: string;
  age: number;
  nationality: string;
}

const user = {
  username: "simple.user",
  password: "aaaaaaa",
};
